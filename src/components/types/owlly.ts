// TODO: Do we want to create a library to handle all our interfaces?
// to avoid duplicates...

export interface Owlly {
  id: string;
  title: string;
  description?: string;
  link: string;
  organisation: string;
}
